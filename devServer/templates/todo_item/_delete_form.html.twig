<form method="post" action="{{ path('todo_item_delete', {'id': todo_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ todo_item.id) }}">
    <button class="btn">Delete</button>
</form>
